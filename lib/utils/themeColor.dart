import 'package:flutter/material.dart';

class ThemeColor {
  //backgraud app
  static const Color background = Color(0xFFE4E4E4);
  //backgraud card home page
  static const Color backgroundCard = Color(0XFFFFFFFF);
  //color to object is selected
  static const Color isSelected = Color.fromARGB(255, 189, 189, 189);
  //color to object is not selected
  static const Color isNotSelected = Colors.white;
  //Color to main font
  static const Color fontPrimary = Colors.black;
  //color cube front
  static const Color cubeFront = Colors.purple;
  //color cube back
  static const Color cubeback = Colors.blue;
}
